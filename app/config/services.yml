# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    BibliographieManager:
      class: AppBundle\Services\BibliographieManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    CollectionManager:
      class: AppBundle\Services\CollectionManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    CommuneManager:
      class: AppBundle\Services\CommuneManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    CoordonneesManager:
      class: AppBundle\Services\CoordonneesManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    DepartementManager:
      class: AppBundle\Services\DepartementManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    EditeurManager:
      class: AppBundle\Services\EditeurManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    LieuDitsManager:
      class: AppBundle\Services\LieuDitsManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    PaysManager:
      class: AppBundle\Services\PaysManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    PeriodiqueManager:
      class: AppBundle\Services\PeriodiqueManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    VilleManager:
      class: AppBundle\Services\VilleManager
      arguments: ["@doctrine.orm.entity_manager"]
      public: true

    SerializerJSON:
      class: AppBundle\Services\SerializerJSON
      arguments: []
      public: true